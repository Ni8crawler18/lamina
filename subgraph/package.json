{
  "name": "laminaa-subgraph",
  "version": "0.1.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ laminaa"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.91.1",
    "@graphprotocol/graph-ts": "^0.38.0"
  }
}
